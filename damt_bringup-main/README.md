# damt_bringup
diese ros2-package ist eine Kopie der emaros_bringup und vereinfacht nur das Starten aller Nodes des Pacman-Spiels

- Diese package ist keine dependencie für irgendeine andere Node und kann ignoriert/gelöscht werden, wenn nicht benötigt

